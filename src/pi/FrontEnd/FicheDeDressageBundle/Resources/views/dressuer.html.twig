{% extends ":FrontEnd:base.html.twig" %}


{% block body %}
    dresseur

    <div class="gallery team">
        <div class="container">
            <h3 class="agileits-title w3title2">Dresser</h3>
            {{ form_start(rech) }}
            <div class=" col-md-4 col-md-offset-4  ">
                <div class="input-group ">
                    {{ form_widget(rech.Recherche, {'attr': {'placeholder': 'Search for... ','class':'form-control'}}) }}
                    {#<input type="text" class="form-control" placeholder="Search for...">#}
                    <span class="input-group-btn">
                     {#<button class="btn btn-default"  type="submit"><a href="{{ path('rech') }}">Go!</a></button>#}
                   </span>
                </div><!-- /input-group -->
            </div>
            {{ form_end(rech) }}
            <div class="agile_gallery_grids">
                {% for  i in veterinaires %}
                    <div class="col-sm-4 col-xs-4 col-md-6 agile_gallery_grid">
                        <div class="w3ls_gallery_grid">
                            <a href="{{ path('front_end_show_dres', { 'id': i.id }) }}" class="lsb-preview wthree_p_grid" data-lsb-group="header">
                                <img src="{{ asset('FrontEnd/images/g1.jpg') }}" alt=" " class="img-responsive" />
                                <div class="agileinfo_content_wrap">
                                    <div class="agileits_content">
                                        <h3>Best Pets  {{ i.email  }} </h3>
                                        <p>Ne nam facilisis adolescens faucibus. {{ i.numTel }}</p>
                                        {#{{ i.idmembre }}#}
                                    </div>
                                </div>
                            </a>
                        </div>
                        </br>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

