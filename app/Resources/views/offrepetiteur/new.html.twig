{% extends ':FrontEnd:base.html.twig' %}

{% block body %}
    <h1>Offrepetiteur creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('reclamation_newOffre') }}">ajouter  une reclamation</a>
        </li>
        <li>
            <a href="{{ path('offrepetiteur_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
